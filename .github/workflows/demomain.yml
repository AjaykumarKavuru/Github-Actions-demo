name: test
'on':
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a simple github action
      - run: echo"this workflow run successfully for that particular action"
